import * as React from 'react';
import type { StyleProp, ViewStyle } from 'react-native';
import { requireNativeView } from 'expo';

export type <%= className %>ViewProps = {
  style?: StyleProp<ViewStyle>;
};

const NativeView: React.ComponentType<<%= className %>ViewProps> = requireNativeView('<%= className %>Module');

export const <%= className %>View = (props: <%= className %>ViewProps) => 
  <NativeView {...props} />;
